node_modules
.pnpm-store
*.log*
.nuxt
.nitro
.cache
.output
.env
dist
.DS_Store
.fleet
.idea
.wrangler
.vercel
coverage

# Agent config: the skills symlinks are shared, everything else is local
# (Claude Code writes .claude/settings.local.json and .claude/worktrees/).
.claude/*
!.claude/skills

# Page snapshots and console logs the Playwright MCP server drops in the repo
# root while an agent drives the app.
.playwright-mcp
